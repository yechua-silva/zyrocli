# openspec/config.yaml
# Auto-generated by sdd-init — 2026-06-13
schema: spec-driven

context: |
  Project: ZyroAgentCLI (zyrocli)
  Stack: Go 1.22+, module github.com/secko/zyrocli
  Architecture: Cobra CLI, internal packages (scheduler, handoff, skilladvisor, spec, context, apply, test)
  Testing: go test (intended — no Go source or test files exist yet; scaffold/project-inception phase)
  Style: The project is at scaffold stage. See AGENT.md for planned architecture.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: "go test ./..."
  verify:
    test_command: "go test ./..."
    build_command: "go build ./..."
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: "No test runner or test files detected yet — project is at scaffold stage. go test is the intended framework once source code exists."
  runner: "go test"
  layers:
    unit: false
    integration: false
    e2e: false
  tools:
    linter: "golangci-lint (planned)"
    formatter: "gofumpt (planned)"
    type_checker: "go vet (planned)"
  coverage: false
