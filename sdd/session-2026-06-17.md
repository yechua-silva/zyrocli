# Sesión 2026-06-17

## Temas tratados

### 1. TUI Redesign (NO TOCAR)
- Quedó como bug conocido. El humano lo arregla.

### 2. Limpieza de sistema
- Eliminado binario engram de ~/.local/bin/
- Eliminado MCP context7 y engram de ~/.claude/mcp/
- Eliminado plugin engram de ~/.claude/settings.json
- Eliminado .codex/config.toml MCP engram
- Eliminado CLAUDE.md de ~/.claude/
- EngramStore Go (internal/memory/) ES CÓDIGO PROPIO, no se toca

### 3. GPU installer automático (FIX)
- scripts/install_tui.py: nuevas funciones _auto_configure_rocm() y _auto_configure_vulkan()
- Post-instalación ejecuta: modprobe amdkfd → persiste módulo → mata ollama → arranca con envs ROCM → espera → verifica backend → guarda env vars en ~/.bashrc

### 4. HelixDB post-install (FIX)
- internal/db/helix/client.go: startHelixContainer() busca helix CLI o docker compose
- EnsureStarted() ahora inicia el container si ping falla
- cmd/zyrocli/install.go: HelixDB step ahora fatal (no non-fatal)

### 5. MCP_DIR_PLACEHOLDER (FIX)
- Código fuente install.go resuelve ruta dinámicamente
- Config existente opencode.jsonc actualizado con ruta real

### 6. Subagentes arreglados (FIX)
- task_manager.go: executeTask ya no ejecuta CLI opencode subagent (no existe)
- Nueva CompleteTask()
- delegate.go: DelegateStep sin CLI
- mcp_server.go: nuevo tool complete_task
- New watcher/ package con polling a HelixDB

### 7. Watcher (NUEVO)
- cmd/zyrocli/watcher.go + internal/watcher/watcher.go
- zyrocli watcher: polea HelixDB → ejecuta subagentes via OpenCode API → notifica al TUI
- Usa endpoints: /session, /experimental/session/{id}/background, /tui/show-toast, /tui/append-prompt, /tui/submit-prompt
- HelixDB queries implementadas (getPendingTasks, getNotifications, markRead)

### 8. Skills Notification
- 14 SKILL.md embebidos actualizados con paso de Notification al terminar
- Tanto los de internal/opencode/skills/ (fuente) como ~/.config/opencode/skills/ (instalados)

### 9. dispatch_task + HelixDB
- handleDispatchTask ahora guarda nodo Task en HelixDB via REST API

### 10. README actualizado
- Nueva sección de Notificaciones Asíncronas
- Integración con OpenCode API HTTP
- Comandos watcher y install

### 11. Investigación OpenCode
- OpenCode v1.17.8
- serve expone REST API + SSE (/event, /global/event)
- Endpoints TUI: /tui/append-prompt, /tui/submit-prompt, /tui/show-toast
- Endpoint experimental: /experimental/session/{id}/background
- OpenAPI spec completa en /doc

## Pendientes para mañana
- Hacer que el watcher sea transparente (que arranque solo con zyrocli install)
- Context7 reemplazado por Context + GitMCP (no usar más)
- Los subagentes ya no fallan, pero el watcher es el que los ejecuta via API HTTP

## Build
- go build ./... OK
- 56 tests pasan
- Binario Go 17MB instalado en PATH (reemplazó versión npm)
